const ROWS : u32 = 10;
const COLS : u32 = 15;

proc main() => int {
    for (var r : u32 = 0; r < ROWS; r += 1) {
        for (var c : u32 = 0; c < COLS; c += 1) {
            #writeout(".", 1);
        }

        #writeout("\n", 1);
    }

    return 0;
}
