proc main() => int
{
    // Because Nibble does not yet support arrays, we use local variables that
    // are allocated sequentially in the stack to test pointer arithmetic.
    var a : int = 1;
    var a2: int = 1;
    var b : int = 2;
    var c : int = 3;
    var d : int = 4;
    var e : int = 5;
    var f : int = 6;
    var p : ^int = ^f;

    //return ^d - p;
    //return *(p + a2 + 1 + a);
    //return *(p + a);
    //return *(2 + p);
    //return *(2 + p - 1);
    //return *(*p + p - 4);
    //return *(7 + p - *p - 1);
    //return *(8 + p - *p - a + 1);
    //return *(7 + p - *p - a + a2);
    return *(7 + ^*p - *p - a + a2);
}

