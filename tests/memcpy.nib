
proc main() => int {
    var a : int = 10;
    var b : int = 0;
    var c : int = -1;

    var size := #sizeof(int);

    #memcpy(^b, ^a, #sizeof(int));

    #memcpy(^c, ^b, size);

    // 10 + 10 = 20
    return b + c;
}
