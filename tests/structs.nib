struct Vec3 {
    x : int;
    y : int;
    z : int;
}

proc main() => int {
    #writeout("Structs\n", 8);

    var v : Vec3;
    v.x = 10;

    var p_v : ^Vec3 = ^v;
    p_v.y = 20;

    (*p_v).z = 30;

    // 10 + 20 + 30 = 60
    return v.x + p_v.y + (*p_v).z;
}
