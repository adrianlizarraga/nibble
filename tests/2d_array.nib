var g_a : []int = {1,2,3,[5]=4};
var g_b : [][2]int = {
    {0, 1}, {2, 3}
};

proc main() => int {
    var a : [2][2]int = {
        {0, 1},
        {2, 3}
    };

    var b : [2][3]char = {
        "aa",
        "bb"
    };

    #writeout(b[1], 2);
    #writeout("\n", 1);

    return a[1][1] + g_a[5] + g_b[1][0]; // 3 + 4 + 2 = 9
}
