import "./cstring.nib";

proc main(argc : int, argv : ^^char) => int
{
    var a : ^char = "Hello\n";
    var num_chars := len(a);

    #writeout(a, num_chars);

    return num_chars + CSTR_CONST + cstr_var + get_rand(1);
}

